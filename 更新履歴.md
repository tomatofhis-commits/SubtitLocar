# SubtitLocar 更新履歴

## アップデート ver0.7 (2026-06-03)
日本語音声認識の特化モデル「Kotoba-Whisper」への対応と、音声認識（STT）モデルの設定をUIから一元管理して多言語でも軽量な `small` などを選べるように機能拡張しました。

### 🎯 音声認識（STT）モデルのUI選択機能と日本語特化モデルの導入
- **「音声認識モデル (STT)」ドロップダウンの追加**: 従来の日本語限定の設定を廃止・統合し、共通設定としてのモデル選択ドロップダウンを追加しました。
- **言語設定との動的な連動制御**:
  - 音声認識の言語で「日本語」を選択した場合は、日本語特化モデル（`Kotoba-Whisper v2.0 (公式)` / `Kotoba-Whisper v2.2 (有志版)`）を含むすべてのモデルが選択可能です。
  - 「日本語以外」または「自動判定」を選択した場合は、特化モデルを除外した標準モデル（`large-v3-turbo` / `small` / `large-v3`）のみが選択肢に表示されるようにし、誤設定を防ぐ設計にしました。
  - 日本語以外に切り替えた際、日本語特化モデルが選ばれていた場合は、自動的に標準の `large-v3-turbo` へフォールバックします。
- **モデルごとの特徴**:
  1. `Kotoba-Whisper v2.2 (日本語特化・有志版)`: `large-v3-turbo`ベースで日本語に特化させ、高精度かつVRAM使用量を半分に抑えた推奨モデル。
  2. `Kotoba-Whisper v2.0 (日本語特化・公式)`: `large-v3`ベースの公式日本語特化モデル。
  3. `large-v3-turbo (標準・高速高精度)`: 高精度でレスポンスが高速な多言語標準モデル。
  4. `small (標準・軽量)`: VRAM（グラフィックメモリ）消費を約0.5〜1.0GBに抑えたい環境向けの超軽量モデル。
  5. `large-v3 (標準・超高精度)`: 高性能GPU向けのフルサイズ標準モデル。

---

## アップデート ver0.6 (2026-06-03)
長時間放置時のクラッシュ不具合を解消し、デフォルトの音声認識（STT）モデルをより高精度で高速な `large-v3-turbo` に変更しました。

### 🛡️ 長時間放置時のクラッシュ対策・堅牢化
- **VADスレッド二重起動の修正**: WASAPIループバック時に無音判定（VAD）スレッドが2重起動していたバグを修正し、音声フレームが競合して欠落する問題を解消しました。
- **メモリリーク対策（キュー送信処理の改善）**: 音声認識の処理遅延時にキューが満杯になった場合、古い音声データを自動的にドロップする非ブロッキング仕様に変更しました。これにより、メモリ上に処理待ちタスクが蓄積されて強制終了（OOMクラッシュ）する問題を完全に回避しました。
- **録音デバイス切断時の自動再接続機能**: Windowsのスリープ復帰や、USBマイク・スピーカーの一時的な切断によって録音APIが例外を吐いた際、2秒おきに自動で再オープンを試みるリトライループを実装しました。これにより、スレッドが途中で死んでハングアップするのを防ぎ、自動復旧が可能になりました。

### 🚀 音声認識（STT）精度の向上とレスポンス改善
- **`large-v3-turbo` モデルのデフォルト採用**: デフォルトの音声認識モデルを `small` から `large-v3-turbo` に変更しました。精度が飛躍的に向上するとともに、従来の `large-v3` と比較して約2〜3倍の高速応答が可能になり、リアルタイムの翻訳字幕レスポンスが大幅に改善されました。

---

## アップデート ver0.5 (2026-05-17)
ゲーム音などのシステム音声を直接キャプチャして翻訳する機能（ループバック機能）を追加し、GPU処理を最適化しました。

### 🎧 システム音声（ゲーム音）の翻訳機能追加
- **キャプチャ方式の切り替え**: 設定パネルに「キャプチャ方式（マイク／システム音声）」の選択項目を追加し、用途に合わせて入力ソースを切り替えられるようにしました。
- **WASAPIループバック対応**: `soundcard` ライブラリを導入し、仮想オーディオケーブル等を使わずに、PC上で鳴っている音（ゲームや動画など）を直接取得して翻訳できるようにしました。
- **システム音声向け最適化**: BGMなどが連続して鳴り続ける環境に対応するため、最大録音区間を短く（15秒→10秒）調整し、録音開始時のバッファ詰まりを防止する仕組みを実装しました。

### 🚀 GPU処理の最適化とビルド修正
- **RTX向け計算設定の最適化**: NVIDIA RTXシリーズの性能を最大限引き出すため、音声認識の計算設定（`compute_type`）を `float16` に最適化し、処理の高速化と安定性を向上させました。
- **スタンドアロンビルドの依存関係修正**: 新規追加したライブラリ（`soundcard`, `cffi`）が実行ファイル（.exe）に正しく同梱されるようNuitkaビルドスクリプトを修正しました。

---

## アップデート ver0.4 (2026-03-16)
音声認識（STT）のハルシネーション（幻覚）対策を強化し、意図しない字幕の誤出力を低減しました。

### 🛡️ 音声認識のハルシネーション（幻覚）対策
- **確信度ベースの自動フィルタリング機能の追加**: マイクが拾った「雑音やノイズ」から、Whisperが「ご視聴ありがとうございました」といった定型句を勝手に出力してしまう現象を低減させるため、`stt_engine.py` に条件判定を追加しました。
- 各セグメントの「無音確率（`no_speech_prob`）」と「認識の確信度（`avg_logprob`）」をチェックし、自信のない結果（＝ノイズ由来の可能性が高いもの）を自動的に画面から弾くように調整しました。
- 閾値は通常発話を阻害しない安全なレベルに設定しており、テキストのブラックリスト化を行っていないため、本当に喋った言葉はそのまま表示されます。

---

## アップデート ver0.3 (2026-02-25)
音声認識精度のさらなる向上と、設定情報の確実な保存・永続化を実現しました。

### 🎯 文字起こし精度 (Beam Size) の調整機能
- **「文字起こしの精度 (Beam Size)」スライダーの追加**: Whisperの探索深度（Beam Size）を最小1〜最大10まで設定可能にしました。値を大きくすることで、より正確で文脈に沿った文字起こしが期待できます（デフォルトは5、数値増で計算負荷も増大します）。

### 💾 設定保存・永続化の堅牢化
- **ウィンドウ閉鎖時（×ボタン）の自動保存**: 設定ウィンドウを右上の「×」ボタンで閉じた際も、現在の設定が即座に `settings.json` へ保存されるように改善しました。
- **スライダー操作の即時保存**: マイク感度や無音判定レベルを変更した際、マウスを離した瞬間ではなく、操作のたびに即設定内容が反映・保存されるよう感度を高めました。
- **起動時のロード処理改善**: 設定ファイルの一部が壊れていても他の正常な項目を読み込めるよう、安全なロードロジック（個別読み込み）を実装しました。

---

## アップデート ver0.2 (2026-02-24)

---

## 初期リリース版 ver0.1 (2026-02-24)
本プロジェクトを開発環境からスタンドアロンで動作する実用アプリとして完成させました。

### 🚀 ビルドと名称変更・安定化
- **正式名「SubtitLocar」の適用**: アプリ名を「ローカルAI字幕システム」から正式名「SubtitLocar」に決定し、設定画面・Web版字幕画面・ドキュメント等を全面的に更新しました。
- **専用アイコンの搭載**: 用意されたオリジナルアイコン (`subtitlocar.ico`) を設定画面のウィンドウ左上および実行ファイル(`main.exe`)のアイコンに適用しました。
- **完全サイレント起動化**: Nuitkaコンパイルにおいて `--windows-console-mode=disable` を適用し、黒いコンソール・ターミナルが一切表示されないバックグラウンドネイティブアプリ化を実現しました。
- **CUDA DLL競合・パス未解決エラーの解消**: 環境内に点在する古いNVIDIA/CUDA DLLがNuitkaビルドのSTTエンジン実行時に混入して強制落ち（Float16非対応エラーなど）する問題を、ビルドスクリプト側のインテリジェントなコピーロジック改善により解決しました。無音時ハルシネーションもVAD調整等により低減を行っています。

### ✨ UI・フロントエンドの改善
- **ステータスインジケーター**: 設定画面に「マイク稼働状況」と「AI翻訳待機状況」の2つのシグナルランプを追加表示し、システムの状態が一目でわかるようにしました。
- **システム終了ボタン**: UI最下段に、プロセスと全非同期タスクを安全に一括シャットダウンするための専用の「システム終了」ボタンを配置しました。
- **Ollamaモデルの動的取得・選択**: 設定画面にて現在ローカルにインストールされている全Ollamaモデル一覧を取得し、任意の翻訳モデルをコンボボックスから切り替えられるようにしました。
- **入出力言語の自由選択**: 同じく設定画面より、音声認識（STT）の「入力言語」、LLMへの「翻訳元言語」「翻訳先言語」の3つをコンボボックスから指定できるように機能拡張しました。

### 🔌 外部連携（SecreAI・他システム対応）機能の拡張
- **WebSocketによる直接テキスト受信の開放**: 音声を使わずに他システムからテキストベースで直接字幕表示指示 (`translate_request`) を受け付けるAPI窓口を開放しました（「secreai_integration_guide.md」を併せて追加）。
- **音声処理の最適化（マイク特化）**: これまでマイクとPC出力音（ループバック）を混合させていましたが、入力元を「マイク」専用へと固定しパフォーマンスを向上。外部からの音声は先の「テキスト受信」へ任せるシステム構成へと整理しました。
- **字幕文字の分割解除と視認性確保**: 外部入力経由で長文が送られてきた際、途中で文字がぶつ切りに分割表示される従来の制限を取り払い、そのままの文章で表示させるバイパスロジックを実装しました。また、入力ソース（マイク由来か外部システム由来か）に応じて字幕のフォントや配色をそれぞれ専用に切り替えるCSS機能も追加しました。
